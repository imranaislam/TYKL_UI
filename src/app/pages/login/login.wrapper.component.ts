import { Component, HostBinding, OnDestroy } from '@angular/core';
import { LoginComponent } from './login.component';

@Component({
  selector: 'ngx-login-wrapper',
  template: `<ngx-login></ngx-login>
  `,
})

export class LoginWrapperComponent {}
